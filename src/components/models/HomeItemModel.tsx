export interface itemPops {
  title: string,
  images: string[],
}

export interface produtItens{
  title: string,
  description: string,
  images: string[],
  price: string
}

export interface detailItem {
  title: string,
  longDescription: string,
  images: string[],
  action: string,
  price: string,
  options: string[]
}